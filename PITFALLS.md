# 踩坑记录（PITFALLS）

> 高频踩坑记录，新人必看。每一条都是用真实bug换来的教训。
> 按类别分，遇到问题先查这里。

---

## 一、挂点/生命周期坑（最高频）

### 1. GameMaster.NewGame 从不触发
**现象**：开新档后 OnNewGame() 不执行，所有状态初始化失败。
**根因**：GameMaster.NewGame 在全ISIL dump中0个调用点，游戏根本没调它。
**正确做法**：挂 `PlayerStore.StartNewGame` Postfix。
**教训**：不要凭名字猜挂点，拆包确认调用链。

---

### 2. 读档后状态叠加/不回退
**现象**：喂食后不打烊退出重进，饱食度还是喂食后的值。
**根因**：static内存缓存跨读档不清空。
**正确做法**：读档时 ClearMemStats()，内存缓存必须在读档时清空。
**教训**：所有static状态在读档时必须重置。

---

### 3. 蛙娘shape读档后变1×1
**现象**：读档后蛙娘占地变成1×1。
**根因**：ApplyIcon() 里的 SetSpriteAndShape() 会根据sprite尺寸自动重算shape，覆盖了之前SetShape(2×3)。
**正确做法**：先ApplyIcon再SetShape，顺序不能反。
**教训**：原生方法可能有副作用，调用顺序很重要。

---

### 4. 蛙娘贴图读档后交替/闪烁
**现象**：读档后蛙娘贴图在新旧之间闪。
**根因**：ResolveSpriteByName 设当前动画帧，但有时序问题。
**正确做法**：多轮修复（ResolveSpriteByName + EnsureSprites + 动画缓存重置）。
**教训**：读档贴图问题经常不是单一原因，需要多管齐下。

---

## 二、状态/持久化坑

### 5. PlayerPrefs 不随档隔离
**现象**：连续开新档，旧档状态残留。
**根因**：PlayerPrefs 是全局的，不随存档隔离。
**正确做法**：走 WageSaveStore（per-slot文件），文件名带runID。
**教训**：跨档状态绝对不能直接用PlayerPrefs。

---

### 6. default_run 残留
**现象**：新档开局读到旧档状态。
**根因**：开局runID是空窗口，写入到default_run，新档读到残留。
**正确做法**：新档时 CleanDefaultRun()，清空default_run残留。
**教训**：开局空窗口是坑，必须清残留。

---

### 7. GetCalorie() 无标签时返回默认300
**现象**：用 GetCalorie() 判断"有没有卡路里"，结果空物品也返回300。
**根因**：GetCalorie() 无tag时返回默认值，不是0。
**正确做法**：查 `CALORIE_VALUE_TAG` / `CALORIE` tag，不要用 GetCalorie() 判空。
**教训**：不要靠返回值判"有没有"，要查tag。

---

## 三、物品/容器坑

### 8. 蛙娘销赃后读档又回来了
**现象**：点销赃 → 蛙娘消失 → 不打烊退出 → 读档 → 蛙娘又出现了。
**根因**：只是设了离开标记，没真删实体，ES3存档里还有。
**正确做法**：打烊前退出一切回退，不要先于打烊保存。
**教训**：所有状态改动只在打烊SaveGame时落盘。

---

### 9. 蛙娘进箱后复制
**现象**：蛙娘收进箱子 → 偷钱外出 → 回归又发一个 → 两个蛙娘。
**根因**：RemoveGirlFromScene 只遍历顶层网格，找不到箱子里的。
**正确做法**：递归翻所有容器，存在性判断走实体优先。
**教训**：找东西必须递归找全，不能只看顶层。

---

### 10. 节点被当成模组吞噬
**现象**：吞噬季把场景节点也吞了。
**根因**：`node_small`/`node_medium` 和 `system_module_*` 同表注册，都有MODULE_TAG。
**正确做法**：吞噬判定排除节点id。
**教训**：原生tag分类不精确，业务上要再过滤。

---

### 11. 骰子工厂兜底返回背包
**现象**：骰子变成带背包标签的容器。
**根因**：`CreateRegisteredDice()` 旧兜底返回 `simple_backpack`。
**正确做法**：工厂兜底返回值要清理标签。
**教训**：兜底/默认值要检查副作用。

---

## 四、环境/工具坑

### 12. Git Bash 残缺
**现象**：`ls`/`grep`/`find` 命令用不了。
**根因**：本机Git Bash不完整。
**正确做法**：用 Glob/Grep 工具，不要用shell的ls/find/grep。
**教训**：Windows环境别硬用bash命令。

---

### 13. Copy-Item -Force 静默失败
**现象**：部署DLL看起来复制了，实际没复制。
**根因**：PowerShell的Copy-Item有时静默失败。
**正确做法**：用 deploy.py（Python shutil.copy2 + MD5校验）。
**教训**：重要文件复制后必须校验MD5。

---

### 14. 增量编译隐藏警告
**现象**：改了代码没报错，但实际有警告。
**根因**：增量编译不重新检查所有文件。
**正确做法**：比对新警告必须 `--no-incremental` 全量重编。
**教训**：要看警告就全量编。

---

## 五、兼容性坑

### 15. 和市场/物品工坊mod冲突爆红
**现象**：我们的mod和市场/物品工坊一起开，游戏爆红。
**根因**：patch冲突，双方挂了同一个方法。
**正确做法**：冲突防护（ManualPatcher让路），或至少让自检看得见。
**教训**：和热门mod冲突要主动测，别等用户报。

---

### 16. 储物区升级mod失效
**现象**：打了蛙哥mod后，储物区升级mod不管用了。
**根因**：我们的容器升级覆盖了对方的。
**正确做法**：容器升级链要兼容，不要硬覆盖。
**教训**：改原生机制时考虑其他mod也在改。

---

## 六、用户体验坑

### 17. 全部特性自动启用
**现象**：新档开局所有特性都生效了。
**根因**：精神错乱特性bug（优先级最高锁死，抽卡逻辑有问题）。
**正确做法**：精神错乱特性单独测，确保点数不够必抽负面。
**教训**：特殊机制特性要单独测边界情况。

---

### 18. 蛙娘好感度养不上去
**现象**：玩了三十多天好感还是十几。
**根因**：好感增长数值太低，或者K_ALLOWANCE_COUNT初始值错了。
**正确做法**：好感增长公式重新设计，初始值单独读不走GetStat。
**教训**：数值平衡要测真实游玩节奏，不能想当然。

---

## 快速排查表

| 症状 | 先查 |
|---|---|
| 新档没反应 | 挂点是不是GameMaster.NewGame？ |
| 读档叠加 | static内存缓存有没有清？ |
| 读档回退失败 | 是不是打烊前退出？ |
| 蛙娘不对 | 先查实体在不在，再查贴图，再查shape |
| 状态不对 | 先查PlayerPrefs，再查WageSaveStore |
| 编译警告不对 | 全量重编了吗？ |
| 部署不对 | MD5校验了吗？ |
| 爆红 | 和其他mod冲突了吗？ |

---

> 遇到新坑记在这里，后来人少踩。
