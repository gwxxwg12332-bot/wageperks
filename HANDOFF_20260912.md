# Wage's Perks 工作交接（2026-09-12 完整版）

> 给接手 AI：本文件是唯一交接入口。接手顺序 = 读本文件 → 读 BUGS.md → git log → 按 §5 待办开工。所有结论来自拆包/日志实证；未实锤标注【待查】。禁止把未验证的猜测写成结论。

---

## §0 项目地图

### 路径
| 项 | 位置 |
|---|---|
| 项目根 | `D:\DoubaoWork\Project_001_WagesPerks\` |
| 源码仓库（git main） | `...\07_开发资产\ProbablyStolen_DevFiles\Mods_开发源码与临时文件\JacksonPerks\` |
| 发布版 DLL（**双目录同步**） | `01_深空当铺汉化包+王尔德补丁\深空当铺汉化包+王尔德补丁\WagesPerks_发布版\` + `04_产物目录` |
| 运行时 DLL | `D:\Steam\steamapps\common\Probably Stolen Playtest\Mods\WagesPerks.dll` |
| 游戏日志 | `...\MelonLoader\Latest.log`（**一切功能排查第一入口**：Failed to patch / 异常都在这里） |
| 经验文档 | `02_豆包产出\DoubaoWork_chats\2026-09-01\new-chat\mod-dev-cheatsheet.md`（**已停止追加**，经验对话内交付） |

### Git 状态
- 远程：wageperks 私有仓库（免密已配）。最近 commit：`385f29d`（显示对齐）。
- 最近部署 DLL：`A3EEC335`（测试中）。发布 tag 先例：`v1.1.5 → e8611a1`（DLL 620032B / SHA256 C6275BF17934B10E）。
- 规范：功能落地 commit（feat:/fix:/refactor:）+ push；发布前 git tag + 双目录同步 + 记录 DLL 哈希。

### 源码文件地图（核心）
| 文件 | 内容 |
|---|---|
| Core.cs（85KB） | **注册中心**：全部 ManualPatcher 注册 + OnUpdate + 各特性混合（30,691 行巨型单文件） |
| Patches.cs（118KB） | 跨特性补丁：博士上货、特殊 NPC 到店、本地化、PerkUI 等 |
| RobinCrusoePerk.cs（172KB） | 鲁滨逊职业全部逻辑（睡眠/拾荒/节点/容器/供应商）+ 通用工具 |
| FrogPowerPerk.cs（256KB） | 蛙哥（Wage Power）：妙妙箱、随机事件、声望、各类 NPC 逻辑 |
| DestinyDice.cs（94KB） | 命运骰子（含报纸翻页实现先例） |
| LuckScoutPerk.cs + LuckScoutBackpackUpgrade.cs | 捡漏直觉（虚空珠/工具箱/探测器） |
| CustomStorageContainer.cs（29.5KB） | 蛙哥妙妙箱容器（注册到 DirectoryMaster，读档自动恢复） |
| ContainerUpgradeV2.cs（16.4KB） | 容器统一升级 v2（段位制 + 满级奖励 + 读档恢复） |
| DrJacksonFriendPerk.cs / AlcoholMerchantPerk.cs / WaterMerchantPerk.cs / RetiredGunsmithPerk.cs / WildeEvidencePerk.cs | 各特性 |
| PerkStatePersistence.cs | 特性持久化（runID 隔离，tag 保存） |
| ManualPatcher.cs | Patch 封装（TryPatch/TryPatchAllOverloads/TryPatchByName） |
| MerchantHelper.cs | AddItemToCounter 上货工具（not_purchased 保留让游戏处理） |
| SpecialNpcManager.cs | 特殊 NPC 到店处理 |
| NewStartTypeUI.cs | 第 14 职业「鲁滨逊的账本」开局 tab（克隆 tabRancher，startType=14） |
| LangHelper.cs | 中英双语 |

---

## §1 特性清单（12 特性 + 1 职业）

| 特性 | 文件 | 关键行为 |
|---|---|---|
| 博士之友 | DrJacksonFriendPerk.cs | 激活即补货（任何开局）；博士到店卖 machine_bay_ext + storage_bay_large（**去掉了神经模组**；**只卖这两个**，不带食品）；上货=AddDirectSellingItemToTable（参数名 gameItem 修过） |
| 刀尖舔血 | RobinCrusoePerk.cs | 每日强制检查；受伤概率 hook（不碰睡眠） |
| 好酒之徒 | AlcoholMerchantPerk.cs | 酒商相关 |
| 笑面虎 | | 顾客心情 |
| 招贼体质 | | 招贼 |
| 霉运缠身 | | 霉运 |
| 信誉扫地 | | 买入卖出 ±20%（文本与实际对齐过；曾出现"买入-20且卖出-20叠加=-40"bug 已修） |
| 命运之骰 | DestinyDice.cs | v2 激活制：双击掷骰=1 事件，value≥threshold，threshold+400，打烊门槛回落，面板卸载按钮（左下角），LastCost×50% 回收；修复过"吃工具"（ItemSelectHandler IsEquipped 排除） |
| 水商之友 | WaterMerchantPerk.cs | 水商蒸发器拦截（AddDirectSellingItemToTable 参数名修过） |
| 酒商之友 | | 酒商 |
| 退休枪匠 | RetiredGunsmithPerk.cs | printer 空物品拦截 |
| 王尔德之手 | WildeEvidencePerk.cs | -50/-500 吸引力/价格 |
| 捡漏直觉（虚空珠） | LuckScoutPerk.cs + LuckScoutBackpackUpgrade.cs | 虚空珠（拖 junk 解锁格子）、工具箱、探测器；**不额外加拾荒次数**（用户拍板） |
| 蛙哥 Wage Power | FrogPowerPerk.cs + CustomStorageContainer.cs | 妙妙箱（3×3 起步螺丝升级→52×10→第二个箱子）+ 随机事件 |
| 鲁滨逊的账本（职业） | RobinCrusoePerk.cs + NewStartTypeUI.cs | startType=14；睡眠/拾荒/节点树/容器升级/屠夫李北文供应商 |

**翻译硬规则**：蛙哥 = **Wage**（不是 Frog）；特性名 Wage Power；Wage's legacy / Wage's Wonder Box；所有 UI/N 网文案统一。

---

## §2 用户拍板决策库（勿自行改动，改前先问）

### 生存数值
- 睡眠：拾荒 -7%（原 15% 用户改的）；打烊 +30% ±10
- 喝水：**5 档真实水质**（WaterHelper.GetWaterPurity）：优质≥9900 +25口/5健/0病/5洁；较好≥9600 +18/2/0/4；普通≥9200 +12/0/5/3；浑浊≥8800 +6/-5/15/1；脏水<8800 +2/-10/30/0；**已删 isStartWater 特判**；自动喝水同步分档；Remove 单位=µl（sip×1000）
- 药品：按基础价值分档 <50 +30 / 50-149 +60 / 150-299 +90 / ≥300 +100（健康满不消耗）；乘 GetDrugEffMult 回光返照
- 自酿酒：按麻醉品价值分档；**>1000 额外档**（出门连续 3 天不受伤 + 加拾荒次数）
- 心情分档：<50 +10 / 50-149 +15 / 150-299 +20 / 300-999 +30

### 容器（最新）
- 蛙哥箱子：3×3 起步 → 拖 nuts_metal（螺丝）逐颗消耗累计 → 段位 5/10/20/40/80 → 满级 52×10 → **发第二个妙妙箱**（TryGiveSecondWageBox，老档满级读档补发，wb_second_given 防重）
- 鲁滨逊容器：开局减半 50% → 拖 junk 逐颗累计 → **满级=原尺寸×2**（GetCrusoeTargetWidth = origW×(0.5+0.3×stage)）
- **翻页机制已整体删除**（双网格方案被游戏睡眠冲突否定，ContainerPageUI.cs 已删）
- tooltip/升级提示已对齐（满级文案：蛙哥"奖励第二个箱子"、鲁滨逊"容量×2"）

### 其他拍板
- 博士：只卖大机器+大箱子（无神经模组、无食品）
- 溶液拦截：酸/碱溶液（CreateAcidBottle/CreateBaseBottle）Patch 返回 null，任何池子不出
- 前 3 天无随机客户：**用户说过"取消前三天不来人的设定"【待确认代码是否已移除】**（原方案：HandleNormalClient/HandleAugClient Prefix 拦截，特殊客户放行）
- 状态面板：固定右上角（anchor=(1,1) pivot=(1,1)，SetPosition(Vector2)=anchoredPosition）
- 屠夫/李北文：并入鲁滨逊职业内；屠夫=肉+近战武器；李北文=梦尘×5+oxycodone_pill×5；第 14/21 天首次上门；电话叫货（8800/8801）

---

## §3 拆包实证库（机制→结论→位置，勿重复拆）

| 机制 | 实证结论 |
|---|---|
| 容器物品读档 | 不走 SetContentWindow（它只挂窗口不填内容）；填回 = `DecodeSaveItem`（PlayerStore.LoadGame 内）→ `PixelWindow.get_child` 挂窗口。读档恢复钩子最稳挂点 = LoadGame Postfix（全部解码完成后） |
| 容器物品存档 | 在 PlayerStore 对象图内（50+ [ES3Serializable] 字段），ES3 反射递归序列化；SaveGame Harmony Prefix 在序列化前（安全） |
| 游戏睡眠 | **会把 buffer（页2）物品移回容器主网格** → 双网格翻页方案失败根因（日志实锤 OnGoingSleep 时 buffer=0） |
| GameItem 结构 | 无 inventory 字段（反射 item.inventory 是死代码）；容器内容 = contentWindow（PixelWindow 0x230）→ childElement → GameGridInventory；inventoryShape public、GridShape.width/height public（P3 类型化） |
| SetShape | 数字重载 `SetShape(int,int)` 缺 ValidateBackground；字符串重载 `SetShape(string,int)` 自动补（虚空珠同路径）——升级/恢复必须用字符串重载（'0'=全开放） |
| 上货权威点 | `PlayerStore.AddDirectSellingItemToTable`（**参数名 gameItem**，Patch 必须同名；水商/枪匠/博士/屠夫李北文全走它） |
| wanted2=屠夫 / wanted6=李北文 | StoreClientListWanted 通缉犯实体（name_the_butcher / name_li_bei_wen；悬赏 450/400；批发商；FACTION_LOWER_LEVEL；李北文货单走 MedsItemDirectory）；走 StoreClientListDict 字典工厂（委托） |
| 电话系统 | StorePhoneClient 字典（InitPhoneClientDict）；`WillAnswerCall(long)` 接听判定；phoneState 0/1 不接、4/5 接；`NoNumber()` 空号提示；红魔鬼=8799/GP矿业=56371 先例 |
| 水造假 | WaterFeatureHelper.GetWaterFeature；CheckSoldDirtyWater = GetBaseFakeDisplay() vs GetBaseRealDisplay()；贴标水=假显示≠真显示 |
| 容器标签 | 蛙哥箱 = CONTAINER_TAG + ITEM_HIDDEN_TAG + SYSTEM_TAG（无 VOID_BEAD_TAG）→ 曾被鲁滨逊容器系统误伤（8 处排除已加 CUSTOM_STORAGE_TAG） |
| Patch 挂载 | Harmony 按参数名绑定；`Parameter "xxx" not found` = 静默失败（Latest.log 查）；ManualPatcher 143 处分散 Core.cs |
| PerkUI | Perk 选择界面打开时禁止重操作/未捕获异常/阻塞（StartingPerkElement.OnPointerClick 曾抛 ArgumentNullException 卡界面） |

---

## §4 踩坑库（勿重复踩）

1. **PowerShell 写 .cs 破坏 UTF-8/换行**（注释吞代码→CS1519）——**改源码一律 python（encoding='utf-8'）或 Edit 工具；禁止 PowerShell Set-Content/Out-File 写源码**。
2. **Patch 参数名必须与原方法一致**（item vs gameItem → 整组 Patch 失败，三个功能一起坏，游戏内无提示只在日志）。
3. **空 catch 数百处**——功能失效放大器；关键路径补日志再查。
4. **诊断日志用完必须删**（不靠 DebugMode 门控；发布前清理）。
5. **翻页方案教训**：任何"把容器物品移出主网格"的设计都会撞游戏睡眠/存档归位——容器容量扩展只走 SetShape（物品留主网格）。
6. **Remove 单位=µl**：sip×1000 才正确扣量（曾误写 sip 导致水不减/误删全瓶）。
7. **读档恢复时机**：LoadGame 时 contentWindow 未初始化（null 静默跳过）——恢复挂 SetContentWindow 或 LoadGame Postfix（物品填回后）。
8. **反射不存在的属性**（item.inventory / w.inventory）静默失败——类型化直读代替。
9. **编译警告数**以 dotnet build 实际输出为准（当前仅 1 个 MSB3270，不是 26）。
10. **测试快捷键放专门测试 mod**，发布版主 mod 不含（F7/F9 等）。

---

## §5 当前待办（接手开工清单）

### P0-① 屠夫/李北文未到店【未修好，最优先】
- 现象：第 14 天屠夫未到店；日志无"已预约屠夫"（有另一功能"供货商已追加水/药/食物 9 件（day 14）"——那是 PostfixPlaceSupplierInventory，**不是**屠夫链）。
- 代码已写+挂点已接：RobinCrusoePerk.cs L2965+（WantedSupplierSchedule→QueueWantedClient→QueueFuturClient("wanted2",0)）；到店 WantedSupplierOnArrived（Patches.cs L798）；电话端注册 Core L28886/L28889；调度挂在 PostfixOnNewDay（=StoreClientManager.OnNewDay Postfix，Core L12804）。
- 【待查，逐个验证不猜】：
  1. **QueueFuturClient 对 wanted 系列是否真生效**（wanted 走 StoreClientListDict 字典工厂，可能不走 futur 队列——拆包确认 QueueFuturClient 的 id→客户查找机制）
  2. 日期索引 0/1 起算（"第 14 天"= index 13 还是 14）
  3. RemoveDuplicateClientsByIdentifier 是否误拦
  4. 玩家档实际到第几天（日志看到 day 14）
- 备选方案（若 QueueFuturClient 不适用）：Patch 客户生成链/字典工厂注入，或直接改 wanted 随机池——**先拆包定**。

### P0-② 能吃喝未拥有物品【未修】
- 根因实锤：DrinkSip（~L1385）/ TreatWithMedicine（~L1396）/ 进食链均无拥有权判定——柜台商品（not_purchased）可双击吃喝。
- 修法：三入口加 `if (item.IsTag("not_purchased") || item.IsTag("TAG_NOT_PURCHASED")) return;`

### P1 收尾验证
- 容器 v2 全链路：新档 3×3→满级→第二个箱子；鲁滨逊 50%→×2；读档恢复/老档迁移；tooltip 文案
- 发布前：诊断日志清理、双目录 DLL 同步、tag、N 网文案（对话交付）

---

## §6 待用户确认（不要自行假设）

1. 屠夫/李北文上门机制方案（拆包定 QueueFuturClient 是否可用，不可用选替代挂点）
2. 鲁滨逊容器要不要也发第二个容器（当前只做容量×2）
3. "前 3 天无随机客户"是否已按"取消"从代码移除
4. 旧翻页测试档物品是否完整（最后一次 Merge 已合并进 main，理论不丢）
5. 博士特性点数：**恢复为 2 点**（曾拍板 +5 又改回，确认最终值；描述同步）

---

## §7 会话内已交付未发布（v1.1.5+ 开发版）

- 容器升级 v2（螺丝/junk 段位制 + 满级奖励第二个箱子）
- 鲁滨逊容器满级 ×2（50%→200% 五段）
- 翻页机制删除 + tooltip/提示/注释对齐 + 删翻页诊断探针
- 屠夫/李北文供应商代码（到店链未通）
- 历史修复（已入 git）：博士上货参数名、容器读档 SetShape 字符串重载、喝水扣量单位、命运骰子双击激活制、溶液拦截、水质 5 档等
